<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.urban.spatium.mapper.UserMapper">


	

	<!--회원리스트 -->
	<select id="getuserList" resultType="User">
		SELECT
		userId
		, userPw
		, userName
		, userBirth
		, userGender
		, userPhone
		, userJob
		, userEmail
		, userRegDate
		, userPoint
		
		FROM
		tbUser AS u
		INNER JOIN
		tbUserLevel	AS l
		ON
		u.userLevel=l.userLevelCode;
	</select>
	
	<!--로그인 -->
	<select id="getUserById" parameterType="String"
		resultType="User">
		SELECT
		 #{userName}
		,#{userBirth}
		,#{userGender}
		,#{userPhone}
		,#{userJob}
		,#{userLevel}
		,#{userEmail}
		,#{userRegDate}
		,#{userPoint};						
		FROM
		tbUser
		WHERE
		#{userId} AND #{userPw} ;
	</select>

	<!--회원가입 -->
	<insert id="addUser" parameterType="User">
		INSERT INTO tbUser
		(userId, userPw, userName, userBirth, userGender, userPhone, userJob, userLevel, userEmail,
		userRegDate, userPoint)
		VALUES (
		#{userId}
		,#{userPw}
		,#{userName}
		,#{userBirth}
		,#{userGender}
		,#{userPhone}
		,#{userJob}
		,#{userLevel}
		,#{userEmail}
		,#{userRegDate}
		,#{userPoint};
	</insert>
</mapper>