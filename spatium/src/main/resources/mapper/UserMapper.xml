<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.urban.spatium.mapper.UserMapper">
	
	
	
	<!--회원탈퇴 -->
	<delete id="removeMyinfo">
		DELETE
		FROM
		tbUser
		WHERE
		userId=#{userId}; 
	</delete>

	<!--회원정보수정 -->
	<update id="modifyMyinfo" >
		UPDATE
		tbUser
		SET 
		  userPw = #{userPw}
		, userName = #{userName}
		, userBirth	= #{userBirth}
		, userGender = #{userGender}
		, userPhone = #{userPhone}
		, userJob = #{userJob}
		, userEmail = #{userEmail}
		WHERE
			userId = #{userId};     
	</update>

	<!--회원삭제 -->
	<delete id="removeUser">
		DELETE
		FROM
		tbUser
		WHERE
		userId=#{userId}; 
	</delete>

	<!--회원수정 -->
	<update id="modifyUser" >
		UPDATE
		tbUser
		SET 
		  userPw = #{userPw}
		, userName = #{userName}
		, userBirth	= #{userBirth}
		, userGender = #{userGender}
		, userPhone = #{userPhone}
		, userJob = #{userJob}
		, userEmail = #{userEmail}
		WHERE
			userId = #{userId};     
	</update>

	<!--회원리스트 -->
	<select id="getUserList" resultType="User">
		SELECT
		  userId
		, userPw
		, userName
		, userBirth
		, userGender
		, userPhone
		, userJob
		, userEmail
		, userRegDate
		, userPoint
		, userGrade
		FROM
			viewUser;
	</select>
	<!-- SELECT userId , userPw , userName , userBirth , userGender , userPhone 
		, userJob , userEmail , userRegDate , userPoint FROM tbUser AS u INNER JOIN 
		tbUserLevel AS l ON u.userLevel=l.userLevelCode; -->


	<!--비밀번호 변경 -->
	<update id="passChange" parameterType="String">
		UPDATE user
		SET
			userPw=#{userPw}
		WHERE
			userEmail=#{userEmail};
	</update>


	<!--로그인 -->
	<select id="getUserById" parameterType="String" resultType="User">
		SELECT
		*
		FROM
		tbUser
		WHERE
		userId = #{userId};
	</select>

	<!-- 아이디 중복 체크 -->
	<select id="idChk" resultType="int">
		SELECT
		COUNT(*)
		FROM
		tbUser
		WHERE
		userId = #{userId};
	</select>

	<!--회원가입 -->
	<insert id="addUser">
		INSERT INTO tbUser
		(userId, userPw, userName, userBirth, userGender, userPhone, userJob, userLevel, userEmail,	userRegDate)
		VALUES (
		#{userId}
		,#{userPw}
		,#{userName}
		,#{userBirth}
		,#{userGender}
		,#{userPhone}
		,#{userJob}
		,#{userLevel}
		,#{userEmail}
		,NOW());
	</insert>
</mapper>